## 2026-03-12
- Added PostgreSQL persistence: tasks/projects now stored in DB `airi` (user/pass airi). Added db.py (psycopg) and init_db() on startup. API uses DB for /projects, /tasks.
- Added GitHub repo auto-create per project using env GITHUB_TOKEN + GITHUB_ORG=airi-company.
- UI updates: theme toggle, gradient backgrounds, improved cards/buttons.
- Phu Quy demo page redesigned into full travel landing (hero, destinations, activities, food, guide, gallery, contact). Code in frontend/app/demo/page.tsx. 
- Added Games page with tic-tac-toe. Sidebar links updated.
- Servers currently running (dev): backend uvicorn on :8000, frontend Next dev on :3000 (set NEXT_PUBLIC_API_BASE to backend).
- Repo moved to org: https://github.com/airi-company/airi.
- Important env in use: GITHUB_TOKEN, GITHUB_ORG, GROQ_API_KEY, HF_API_KEY, PG_DSN (default postgresql://airi:airi@localhost:5432/airi).
- New request: in Vietnamese, daily gold price update at 07:00 (UTC+7) from giavang.org; remind to checkout/go home at 17:30 (UTC+7); add 11:55 (UTC+7) lunch reminder.
