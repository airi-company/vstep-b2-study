## 2026-03-14
- Set up OpenVPN server (UDP 1194) on host 72.155.88.236 using easy-rsa; generated client profile `airi` and created `/tmp/airi-clean.ovpn` (base64 provided to user). Enabled IP forwarding, added iptables MASQUERADE, started openvpn@server (status OK). Opened ufw UDP 1194; user still must open NSG/firewall inbound 1194 on cloud side.
- code-server/nginx stopped previously; HTTPS self-signed attempt caused webview issues; decision deferred.
- Cloned and pushed `pixel-agents` to org `airi-company` (branch main). Mock webview served earlier but user wanted real VS Code extension; code-server approach blocked by cert/service worker; plan was to use ngrok, but lacking authtoken.
